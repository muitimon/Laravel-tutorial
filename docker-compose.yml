web:
    image: hitalos/laravel:latest
    ports:
        - 80:80
    volumes:
        - ./:/var/www
    links:
        - db
    environment:
        DB_HOST: db
        DB_DATABASE: dbname
        DB_USERNAME: username
        DB_PASSWORD: p455w0rd
        DB_CONNECTION: mysql
db:
    image: mysql
    environment:
        # with mysql
        MYSQL_DATABASE: dbname
        MYSQL_USER: username
        MYSQL_PASSWORD: p455w0rd
        MYSQL_ROOT_PASSWORD: p455w0rd
        MYSQL_ALLOW_EMPTY_PASSWORD: p455w0rd
        MYSQL_RANDOM_ROOT_PASSWORD: p455w0rd
